# Folio 제품 기획 v3

## 1. 제품 정의

Folio는 자기소개서를 대신 작성하는 AI 서비스가 아니다. 여러 버전의 이력서와 포트폴리오에 흩어진 정보를 하나의 검증된 커리어 데이터로 통합하고, 그 데이터를 ChatGPT·Gemini 등 원하는 LLM에서 재사용하도록 돕는 개인용 취업 워크스페이스다.

핵심 문장:

> 이력서를 모으고, 사실을 확인하고, 어디서든 쓸 수 있는 내 커리어 데이터로 만든다.

## 2. 해결하려는 문제

- 직무와 시기마다 다른 이력서가 여러 파일에 흩어져 있다.
- 회사명, 기간, 성과 수치가 파일마다 달라 어느 것이 최신인지 확인하기 어렵다.
- ChatGPT에서 자기소개서를 작성할 때마다 이력과 프로젝트를 다시 설명해야 한다.
- LLM에 원본 전체를 바로 넣으면 개인정보 노출과 사실 왜곡 여부를 확인하기 어렵다.
- 지원 현황, 일정과 최종 문서는 별도 도구에 나뉘어 있다.

## 3. 제품 원칙

1. AI 결과보다 사용자 확인을 우선한다.
2. 모든 커리어 항목은 원본 출처를 가진다.
3. 확인 완료된 정보만 기본 내보내기에 포함한다.
4. 개인정보는 기본적으로 LLM 내보내기에서 제외한다.
5. API가 없어도 텍스트 붙여넣기와 직접 입력으로 사용할 수 있다.
6. 모바일에서는 다음 행동 하나가 명확하게 보여야 한다.
7. 자기소개서 생성은 외부 채팅에 맡기고 Folio는 정확한 입력 데이터와 최종본 관리에 집중한다.

## 4. 핵심 사용자 흐름

```text
PDF·텍스트 원본 등록
→ 커리어 항목 자동 추출
→ 날짜·수치·내용 검토
→ 확인 완료 또는 제외
→ 개인정보 포함 여부 선택
→ Markdown/JSON 복사 및 다운로드
→ ChatGPT 등에서 자기소개서 작성
→ 완성된 문서를 회사별로 Folio에 저장
```

## 5. 정보 구조

### 모바일 하단 메뉴

1. 홈
2. 지원
3. 일정
4. 커리어

면접, 공고 보관함과 지원 문서는 세부 메뉴에서 접근한다.

### 커리어 데이터 보관함

#### 1단계: 원본 모으기

- PDF 이력서 업로드
- 이력서·포트폴리오·경력 메모 텍스트 붙여넣기
- 원본별 추출 항목 수와 검토 상태 표시
- 원본 열기, 다시 분석, 삭제

#### 2단계: 내용 확인하기

- 기본 프로필 편집
- 학력, 경력, 프로젝트, 기술, 자격증, 어학, 수상·활동 분류
- 검토 필요, 확인 완료, 제외 상태
- 원본 출처 표시
- 중복 가능성 표시
- 항목 직접 추가·수정·삭제
- 개인정보 표시

#### 3단계: LLM에 가져가기

- 확인 완료된 정보만 기본 포함
- 검토 중 정보 포함 선택
- 개인정보 포함 선택
- Markdown/JSON 전환
- 미리보기, 전체 복사, 파일 다운로드
- LLM이 사실을 만들지 않도록 사용 규칙 자동 포함

## 6. 가이드 UX

- 커리어 화면 상단에 항상 `사용 가이드` 버튼을 둔다.
- 가이드는 원본 등록, 검증, 중복 정리, LLM 사용 순서를 설명한다.
- 빈 상태에서는 설명보다 바로 실행할 버튼을 먼저 보여준다.
- 각 단계에는 등록 수, 검토 필요 수, 내보내기 준비 상태를 표시한다.
- PDF 추출 실패 시 이유를 숨기지 않고 텍스트 붙여넣기라는 대안을 안내한다.
- 개인정보는 기본 제외이며 사용자가 명시적으로 켜야 한다.

## 7. 데이터 모델

### CareerSource

- `id`
- `name`
- `type`: `resume | portfolio | career-note`
- `attachmentId` 또는 `rawText`
- `status`: `ready | review | complete | needs-text`
- `extractedAt`

### CareerFact

- `id`
- `category`
- `title`, `organization`, `period`
- `description`, `achievements`, `skills`
- `sourceIds`
- `status`: `review | verified | excluded`
- `sensitive`

기존 프로필의 학력, 경력, 프로젝트, 자격, 어학, 수상과 기술은 첫 접근 시 `verified` 커리어 항목으로 한 번만 이전한다.

## 8. AI 역할

AI가 하는 일:

- PDF와 텍스트에서 확인 가능한 사실을 구조화
- 날짜, 수치와 고유명사를 원문 그대로 보존
- 학력, 회사, 프로젝트와 자격을 개별 항목으로 분리
- 공고 핵심 요구사항 추출

AI가 하지 않는 일:

- 없는 경험과 수치 생성
- 사용자의 확인 없이 정보 확정
- Folio 내부에서 범용 채팅 제공
- 자기소개서 자동 제출

## 9. MVP 완료 기준

- 여러 원본을 등록하고 삭제할 수 있다.
- OpenAI 연결 시 PDF와 텍스트를 구조화할 수 있다.
- API가 없어도 텍스트 원문과 직접 입력으로 진행할 수 있다.
- 추출 항목을 검토·확인·제외하고 수정할 수 있다.
- 각 항목에서 원본을 확인할 수 있다.
- Markdown과 JSON을 복사하거나 다운로드할 수 있다.
- 개인정보가 기본 내보내기에서 제외된다.
- 사용 가이드와 단계별 빈 상태가 있다.
- 모바일에서 가로 스크롤 없이 핵심 흐름을 완료할 수 있다.
- 기존 지원, 일정, 문서와 Google 로그인이 그대로 동작한다.

## 10. 후속 과제

- 동일 경력의 필드 단위 충돌 비교와 병합 화면
- PDF OCR 및 표 형식 인식 강화
- 원본별 버전과 변경 이력
- 특정 공고에 맞춘 커리어 데이터 부분 내보내기
- ChatGPT·Gemini용 프롬프트 템플릿 저장
- PostgreSQL과 객체 스토리지 이전
- 데이터 암호화, 백업과 복구 정책
- 실제 사용자 이력서로 추출 정확도 평가
